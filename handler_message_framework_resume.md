# 异步消息调度框架（bs_tools Handler）

作为该项目核心成员，我专注于解决割草机业务层多源异步事件与全局状态机协同中的线程调度与任务串行化难题，保障状态切换的线程安全与低延迟响应。

---

## 异步消息调度框架

1. 主导设计并实现仿 Android 的异步消息调度框架，采用 Handler + Looper + MessageQueue 三层架构，适配割草机全局状态机在定时器等多源异步事件下的高可靠、低延迟调度需求。通过统一异步投递与单线程串行执行，避免多线程直接并发修改状态机，有效降低业务侧跨线程调用复杂度。

2. 实现了即时投递、延迟调度、按消息编号分发等多种任务提交方式。系统根据不同业务场景选择对应策略：全局状态机以即时投递为主，将状态切换请求统一排队到专用工作线程执行；延迟任务按执行时间插入消息链表，到期后自然调度，无需额外定时器模块。

3. 基于 eventfd + epoll 实现高效阻塞唤醒机制，替代传统忙轮询方案。消息队列为空或任务未到期时线程阻塞休眠；入队时区分「插到队头则按需唤醒」「按时间排队则不唤醒」两种策略，避免无效唤醒。阻塞等待期间不持有队列锁，兼顾低 CPU 占用与及时响应。

---

## HandlerThread 模块

1. 封装后台线程生命周期管理，对外提供统一的启动、退出、获取调度器接口，内部自动创建并关联 Looper（消息循环）与 MessageQueue（消息队列），实现「一个后台线程 + 一个消息循环」的标准模式。

2. 采用互斥锁 + 条件变量实现线程启动同步，保证业务层获取调度器时后台线程已就绪，避免竞态初始化。

3. 支持优雅退出：设置退出标记并唤醒阻塞中的消费者，等待工作线程安全结束后再返回，业务模块可按需关停。

---

## MessageQueue 模块

1. 采用按执行时间排序的消息链表，支持即时任务与延迟任务混合入队与调度。

2. 设计生产者-消费者锁分离策略：操作队列时加锁，阻塞等待在锁外执行，避免消费者睡眠时阻塞生产者入队。

3. 通过阻塞状态标记 + eventfd 精准唤醒，仅在消费者休眠且队头发生变化时通知，保证唤醒的必要性与高效性。

---

## Handler / Looper 模块

1. Handler 绑定 Looper 并共享其 MessageQueue，统一承担任务投递与执行入口；Looper 在专用线程中循环取消息并分发，保证同一队列内任务严格串行。

2. 采用 Runnable 虚接口解耦框架与业务逻辑，业务侧只需封装具体任务并投递，框架负责线程切换与调度执行。

3. 支持同一 Looper 挂载多个 Handler，消息携带目标 Handler 进行分发，提升框架扩展性。

---

## 全局状态机协同（mower_fsm）

1. 将 HandlerThread 作为全局状态机专用工作线程，定时器等任意来源的状态切换经 GlobalFsm::fire 异步投递后，在该线程中串行调用状态机引擎完成迁移，避免多线程竞态。

2. 与 stateless4cpp 状态机引擎职责分离：调度框架负责事件串行化与线程隔离，引擎负责 permit / onEntry / onExit 等转移规则配置与执行，提升可维护性与可扩展性。

3. 典型落地如方向校准（DirectionInit）等状态：进入时启动业务定时器推进流程，完成或失败时通过 fire 触发状态跳转，由调度框架保证切换在状态机线程中安全执行。

---

## 简历一句话版本

> 作为项目核心成员，设计并实现仿 Android Handler 的异步消息调度框架（eventfd + epoll 阻塞唤醒 + 时间排序消息队列），应用于割草机全局状态机，实现多源异步事件单线程串行处理，保障状态切换线程安全。
