# 面向复杂地形多轮腿式平台的 ROS2 运动控制中间件

**2025 年 — 2026 年　|　学生负责人 / 核心开发者**

作为该项目核心成员，我主导设计与实现了基于 ROS2 Humble 的 L1~L3 分层运动控制栈，面向窄/宽轮距切换、多模式转向与原地自转等复杂工况；以 **Service 表达意图、Action 执行长任务、Topic 承载连续控制**，配合统一配置与 QoS 构建、Trigger FSM 与唯一命令出口，完成 Webots 仿真与真机可替换 IO 的统一架构，保障多模块协同下意图清晰、执行可追踪、控制链路可扩展可维护。

---

## ROS2 混合通信与接口设计

1. 主导设计 **Motion / Track 双域通信模型**：转向/自转与轮距意图分域表达、互不耦合；上层只更新意图，下层按当前状态执行，避免同一接口既下指令又阻塞等待执行完成。
2. 按任务性质选型 ROS2 通信，形成清晰闭环：
   - **Service**：人机侧一问一答写入意图（转向/自转、窄/宽轮距），立即返回，不占用长过程
   - **Topic**：承载连续控制流与状态广播（方向盘、期望姿态、关节命令、轮速、当前模式）
   - **Action**：承载轮距伸缩等长时任务；状态机侧为客户端、执行层为服务端，支持进度反馈、取消与失败回滚；切轨时绕过转向映射节点，由执行层统一仲裁下发
3. 自定义消息层统一 8 关节转向链与 4 轮速链的数据语义（有效标志、来源、时间戳等）；Service 与 Action 接口分离以避免类型冲突；话题/服务/Action 名称集中维护，降低联调与重构成本。
4. 将 Action 客户端封装为独立模块（目标生命周期、取消、结果回调），使状态机主逻辑与通信细节解耦，便于后续扩展自转等长任务（姿态就绪门控、轮速使能等）。

---

## 公共配置与 QoS 模块

1. 建立**全栈统一参数源**并由 Launch 注入各节点：共享参数全局可见，节点专有参数分域管理；实现 **QoS 参数构建机制**，按 Topic 端点从参数生成 QoS（如 `reliable` / `sensor_data`），使控制指令可靠送达、高频传感流只取最新，策略调整仅需改参、无需改业务代码或重编译。
2. 建立关节索引单一数据源（转向 8 关节 / 驱动 4 轮 / 仿真 12 电机三套下标），避免各节点重复维护关节表或索引混用。

---

## FSM 状态机与 Trigger 调度

1. 设计 **六态显式 Trigger 驱动状态机**（窄轨/切宽中/宽轨/切窄中/原地左旋/原地右旋）：操作意图与当前状态解耦，仅在 Trigger 条件满足时迁移，保证工况切换可解释、可追踪。

---

## 执行与仲裁层

1. 设计 **唯一关节命令出口**：全栈仅执行层向硬件下发最终腿命令；转向节点只输出期望姿态，切轨在执行层内由 Action 插值完成，两路来源在同一节点仲裁后统一发布，避免多节点争用执行器。
3. 内置命令看门狗：期望姿态超时未刷新时自动归零下发，防止无操作输入时保持危险姿态。

---

## 其他业务节点与算法层

1. **运动学算法库（纯 C++）**：实现窄/宽轮距曲率映射、原地自转映射与曲率换算，与 ROS 解耦，供转向控制节点调用。
2. **硬件桥接层（L1）**：对接 Webots 完成关节命令/轮速下发与关节状态、IMU 回传；真机侧可替换为阀控/PLC 驱动，上层控制栈无需改动。

---

## 相关文档

- [steering_flow.md](steering_flow.md) — 转向控制数据流
- [ros2_communication.md](ros2_communication.md) — ROS2 通信机制说明
- [架构说明.md](架构说明.md) — 分层架构对照
