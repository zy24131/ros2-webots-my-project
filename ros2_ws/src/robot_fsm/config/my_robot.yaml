actuator_executor:
  ros__parameters:
    position_tolerance: 0.05
    wide_neutral_angle_deg: 45.0
    command_timeout_ms: 450

mobility_controller:
  ros__parameters:
    wheel_speed: 0.0
    track_half_width_narrow: 1.07
    track_half_width_wide: 1.25

steering_controller:
  ros__parameters:
    wide_neutral_angle_deg: 45.0
    steering_deadband_deg: 3.0

robot_fsm:
  ros__parameters: {}
