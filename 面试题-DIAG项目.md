# DIAG 项目面试题与参考答法

面向简历项目「交换机/FE 端口 DIAG 诊断与参数下发框架」。  
答题原则：能画链路、能分清三块职责、不夸大「从零发明架构」。

---

## 0. 开场：项目一分钟介绍

**Q：简单介绍下这个项目？**

**参考答：**  
这是一个交换机端口诊断侧的 C 语言框架，目标是把运维输入的文本命令落到芯片参数。我把它串成一条链路：

1. **表驱动命令树**：解析 `set port 0/96 tx main=50,pre=-1`，路由到处理函数；  
2. **稀疏更新**：只改命令行里出现的 `key=value`，其余字段保持原值；  
3. **分层驱动分发**：经业务 → 适配 → 函数指针表 → 5993 PortDrv/MACsec → 驱动 API。  

TX 路径上还有 **两次下发容错**，应对部分芯片 SerDes 重初始化后参数丢失。核心不是堆 `if-else`，而是用表和函数指针把「解析、改哪些字段、下到哪颗芯片」解耦。

---

## 1. 全链路与职责边界

**Q：从敲一行命令到硬件生效，中间经过哪些步骤？**

**参考答：**

```text
用户输入
  set port 0/96 tx main=50,pre=-1
        │
        ▼
分词 → 按 unit 选 g_pstFeDiagMgmt[unit]
        │
        ▼
fe_diag_walk：set → port → 0/96 → tx
        │
        ▼
fe_diag_dispatch_parms：提 item / side / devId
        │
        ▼
PORT_DIAG_TxParamSet
  ├─ Get 当前参数到 info
  ├─ PORT_DIAG_ParseEqualAdd（稀疏改 main/pre）
  └─ NpLswTxParamSet（两次 Set）
        → Adapt → NP_PORT_DISPATCH_SET_PORT_TXPARAMS
        → Np5993PortDrvSetTxRxParams → SerDes 驱动
```

**Q：命令树、Sparse、分层挂钩子分别解决什么问题？别混在一起说。**

| 模块 | 解决什么 | 不解决什么 |
|------|----------|------------|
| DIAG 命令树 | **调谁**（路由到哪个处理函数） | 不决定改 info 哪些字段 |
| Sparse | **改哪些字段**（只更新出现的 key） | 不负责选芯片实现 |
| 函数指针分层 | **怎么下到哪颗芯片** | 不管命令怎么分词 |
| 两次下发 | **为何发两遍**（容错丢参） | 不是 Sparse 的一部分 |

面试时主动说清这张表，容易显得条理清楚。

**Q：为什么要分层，直接在 `PORT_DIAG_TxParamSet` 里调驱动不行吗？**

**参考答：**  
诊断命令、板级适配、芯片差异、寄存器/SerDes API 变化频率不同。若业务直接调驱动，每加一颗芯片或改一次寄存器封装都要改业务。用 `g_fnNpPortDriver[unit].set_port_txparams` 注册，业务只依赖稳定接口，芯片差异收敛在注册与 PortDrv 实现里。

---

## 2. DIAG 命令树

**Q：核心数据结构是什么？怎么组织成树？**

**参考答：**  
两级结构：

- `FE_DIAG_CMD_TABLE_T`：节点。中间节点挂 `pSubTable`，叶子挂 `pFunDes`。  
- `FE_DIAG_FUN_DES_T`：叶子执行元数据——`pFunc`、`usFlags`（如 `FE_DIAG_USE_UNIT`）、`usParmNum`、`aucParmType[]`。  

根表挂在 `g_pstFeDiagMgmt[unit]->pstCmdList`。扩展新命令主要是：定义 FunDes → 挂进子表 → 父表引用，而不是在解析器里加一大段分支。

**Q：`fe_diag_walk` 怎么匹配？`0/96` 这种 token 怎么处理？**

**参考答：**  
按 token 在当前表里比 `acCmdName`；匹配则下钻子表或命中叶子。若路径要求 `FE_DIAG_USE_UNIT`，遇到形如 `u/p` 的 token 就解析出 unit/port，再继续。  
例：`set port 0/96 tx` → unit=0, port=96，叶子是 TX 的 FunDes。

**Q：参数类型分发做什么？业务函数为什么不自己 strtok？**

**参考答：**  
`fe_diag_dispatch_parms` 按 `aucParmType[]` 消费叶子后的剩余 token（`NUM`/`STR`），并带缺省约定（叶子名 `tx`→`LSW_SEND`，`rx`→`LSW_RECV`，`devId` 默认 0）。  
好处是「怎么提参」集中在框架；业务函数只关心语义参数，避免每个命令复制一套解析。

**Q：多 unit 怎么管？为什么每个 unit 一份管理块？**

**参考答：**  
`g_pstFeDiagMgmt[MAX_UNITS]`，初始化时各 unit 挂根表（可共享同一份表数据）。命令里的 unit 用于选管理实例。多 unit 场景下端口空间、后续扩展（不同 unit 不同表）都更清晰；演示里多 unit 共用同一根表即可。

**Q：和写一堆 `strcmp`/`if-else` 比，表驱动的优缺点？**

| 优点 | 代价/注意点 |
|------|-------------|
| 加命令主要改表，解析器稳定 | 表要维护正确，漏挂则命令「消失」 |
| 用法、参数类型与函数绑定在一起 | 调试要会沿着表查，不能只看业务函数 |
| 多模块可并行注册子树 | 命令名冲突、深度过深要规范 |

**Q：若命令写错（未知关键字 / 缺参），框架应怎样表现？**

**参考答：**  
walk 匹配失败应返回明确错误并尽量带 usage；dispatch 参数个数/类型不符也应失败，不要带着垃圾参数调业务。产品上通常配合 `acUsage` 打印帮助。面试可补一句：错误路径和成功路径一样要测。

---

## 3. 稀疏更新（Sparse）

**Q：Sparse 是什么？和命令树是什么关系？**

**参考答：**  
Sparse 不是另一棵命令树，而是处理函数内部的 **表驱动局部写回**：对 `item="main=50,pre=-1"` 只更新 `info.main`、`info.pre`，未出现字段保持 Get 出来的原值。  
命令树负责把你带到 `PORT_DIAG_TxParamSet`；Sparse 负责「info 里改哪几项」。

**Q：为什么必须先 Get 再 Sparse 再 Set？直接 Set 行不行？**

**参考答：**  
若结构体先清零再解析，未提及字段会变成 0，等于误配置。流程必须是：

1. Get：读出当前完整配置；  
2. Sparse：只改出现的 key；  
3. Set：把完整 `info` 下发。  

这是安全性和运维体验的核心。

**Q：Sparse 算法怎么走？为什么说它是「稀疏」的？**

**参考答：**  
外层按逗号切段（`PORT_DIAG_ParseComma`），内层用 `keyMap` 做 `ParseEqualKey` 匹配；命中则 `parseAfter` 转数值，再 `parseKeyGetFun` 写回。  
稀疏语义来自：**循环跟着命令行 pair 走，而不是扫结构体全部字段强制赋值**。没出现的 key 根本不会进写回回调。

**Q：新增一个配置项要改哪里？**

**参考答：**  
在对应 item 的 `keyMap` 增加一项（key 名、value 解析、写回），必要时补 `info` 字段。业务处理函数签名通常不用改，仍收一个 `item` 字符串。

**Q：非法 key、空 value、超长段怎么处理？**

**参考答：**  
段长校验（如 `a=b` 至少 2、上限约 30）；keyMap 全未匹配则 Invalid key 返回错误；value 解析失败走错误信息记录。面试强调：**失败应让整次 Set 失败或明确部分失败策略，避免 silent ignore 导致以为生效了。**

**Q：Sparse 和「全量 CLI 配置」怎么选？**

**参考答：**  
诊断/微调场景适合 Sparse（只改 FIR 主游标等）；整端口初始化、模板下发可能更适合全量或「先 clear 再 set」。本项目面向运维诊断，Sparse 更合适。

---

## 4. PortDrv 分层与两次下发

**Q：TX 参数下发分层怎么划分？**

**参考答（自上而下）：**

```text
PORT_DIAG_TxParamSet          诊断业务
  → NpLswTxParamSet           业务封装（含两次下发）
    → AdaptLswDispatch...     适配/板级
      → NP_PORT_DISPATCH_...  宏：查 g_fnNpPortDriver[unit]
        → Np5993PortDrv...    芯片 PortDrv（可多 lane）
          → chip_serdes_...   驱动 API
```

**Q：函数指针表和宏分发怎么设计？**

**参考答：**  
`g_fnNpPortDriver[unit].set_port_txparams` 在初始化注册为 5993 实现；业务侧只调 `NP_PORT_DISPATCH_SET_PORT_TXPARAMS`，内部空指针检查后调用。换芯片或同板多 unit 不同能力时，改注册即可。

**Q：为什么要两次下发？一次不够吗？**

**参考答：**  
部分芯片在配 TX 参数时会触发 SerDes 重初始化，第一次写入可能被冲掉或未真正落到生效路径。`NpLswTxParamSet` 里先走适配分发，再经 `ConfigPortSetTxParamByCmd` 同类路径重发一次（最终都落到同一 dispatch 宏）。  
这是 **针对已知硬件行为的容错**，不是业务语义上要配两套参数。面试可主动说：有成本（耗时、日志噪音），应用在确认存在丢参的芯片路径上，并最好有可观测性（两次返回值都检查）。

**Q：两次下发失败怎么处理？第一次成功第二次失败呢？**

**参考答：**  
应检查两次返回值；若第二次失败，状态可能不确定，需要日志、必要时 Get 回读校验，或告警让运维重试。不能假设「发了就一定生效」。

**Q：和 Linux 驱动里的 ops 表有什么相似之处？**

**参考答：**  
非常像 `file_operations` / `net_device_ops`：上层调稳定接口，底层用函数指针落到具体实现。本项目是用户态/平台软件侧的同一思想，便于多芯片并存。

---

## 5. MACsec 注册（挂钩子）

**Q：MACsec 怎么和 PortDrv「同一套思路」？**

**参考答：**  
初始化 `Np5993MacsecFuncRegister(&g_fnNpPortDriver)`，把 `np_create_sc` 等指针填进驱动表；业务 `NpPortXsecCreateSC` → 适配层 → `fnDriver->np_create_sc`。  
业务域不同（端口 TX vs 安全会话），扩展方式相同：**注册挂钩子，运行时调接口**。

**Q：这算策略模式吗？**

**参考答：**  
可以这么讲：驱动表是策略接口，5993 实现是具体策略，注册是注入。也常说「驱动注册 / 挂钩子」。面试用其中一个说法即可，重点是解耦和可替换。

**Q：若某芯片不支持某个 MACsec 能力怎么办？**

**参考答：**  
对应函数指针可置空，适配层调用前判空并返回「不支持」；或挂统一 stub。避免业务层 `#ifdef` 铺天盖地。

---

## 6. 设计权衡与对比题

**Q：为什么不用哈希表做命令查找，而用线性表 + 树？**

**参考答：**  
诊断命令空间通常不大，线性匹配实现简单、可预测，和表项声明式注册匹配。若命令量极大或热路径极致，可再在层内加哈希；对本场景树 + 线性扫足够。

**Q：递归 walk 有没有栈深度问题？**

**参考答：**  
命令树深度由产品约定（一般很浅，如 set/port/u/p/tx 几层）。若担心，可改成循环栈或限制最大深度。演示与产品深度都远小于风险区。

**Q：全局 `g_pstFeDiagMgmt`、`g_fnNpPortDriver` 的线程安全？**

**参考答：**  
典型假设是诊断单线程或配置锁外置。若多线程并发 DIAG，需要对管理块/驱动表的初始化与调用约定（写锁注册、读锁调用，或规定仅主线程跑 DIAG）。简历项目可如实说：当前 demo 按单线程诊断路径设计，产品化需补并发策略。

**Q：和 click / 业界 CLI 框架比，你这套定位是什么？**

**参考答：**  
不是通用 shell，而是 **嵌入式/交换机诊断通道上的轻量表驱动框架**：强调 unit/port、与 Sparse 和芯片 ops 表打通。定位是平台内部可扩展 DIAG，不是替代完整网管。

---

## 7. 场景题 / 排障题

**Q：配了 `main=50`，Get 回来还是旧值，怎么查？**

**参考答（分层排查）：**

1. 命令是否命中正确叶子（日志/断点看是否进 `TxParamSet`）；  
2. Sparse 是否改到 `info.main`（改前改后打日志）；  
3. Set 两次返回值是否都 OK；  
4. 是否发到错误 unit/port/lane；  
5. 芯片是否重初始化冲掉（对应两次下发动机）；  
6. 最后用硬件侧读回或示波器/眼图工具确认。

**Q：只想改 `pre`，结果 `main` 也变了，可能原因？**

**参考答：**  
Get 失败用了未初始化 `info`；Sparse 写错字段；Set 路径用了错误缓冲；或驱动 API 全量寄存器写时带了默认值。优先查 Get/Sparse 前后 `info` 快照。

**Q：如何单测 Sparse，不依赖真芯片？**

**参考答：**  
构造 `info` 初值 → 调 `PORT_DIAG_ParseEqualAdd` → assert 仅目标字段变化。命令树用字符串用例测 walk/dispatch。分层用函数指针换成 mock 实现统计调用次数（尤其验证「两次下发」）。

**Q：演示工程里各文件角色？**

| 文件 | 角色 |
|------|------|
| `diag_demo.c` | 命令树、unit、walk、dispatch |
| `sparse.c` | `PORT_DIAG_ParseEqualAdd` 稀疏写回 |
| `layered_portdrv.c` | Get/Sparse/Set、两次下发、PortDrv ops |
| `layered_macsec.c` | MACsec 函数指针注册与调用 |

---

## 8. 行为与项目边界（高频）

**Q：你在项目里具体负责什么？哪些是平台已有能力？**

**参考答（建议口径）：**  
按平台已有 DIAG/驱动分层思路，我侧重把 **命令解析、稀疏更新、分层下发** 串成可演示的完整链路，并实现/整理表结构、Sparse 解析、PortDrv/MACsec 挂钩子示意与两次下发容错。表述用「实现并串联」，避免「从零发明交换机架构」。

**Q：最大难点是什么？**

**参考答可选其一，讲透即可：**

- 把「路由 / 改字段 / 下芯片」边界划清，避免一个函数包办；  
- Sparse 与 Get/Set 顺序，防止误覆盖；  
- 两次下发这类「硬件行为驱动的软件补偿」需要日志和回读验证。

**Q：如果重新做，你会改什么？**

**参考答示例：**  
统一错误码与 usage 输出；给 dispatch/Sparse/Set 加可开关的 trace；对 ops 表做能力位图；并发与单元测试补齐；命令注册改成显式 `register` API，减少手改多张静态表的漏挂。

**Q：能量化收益吗？**

**参考答：**  
没有压测数据就不要编「性能提升 70%」。可说：新命令/新 key 以加表项为主；误覆盖类配置风险通过 Sparse 从机制上降低；多芯片通过注册扩展而少改业务调用点。

---

## 9. 可能的追问速查（短答）

| 问题 | 一句话 |
|------|--------|
| FunDes 里 `usParmNum` 含不含 unit/port？ | 通常指叶子后的**额外**参数；unit/port 由 `FE_DIAG_USE_UNIT` 在 walk 阶段解析。 |
| `item` 是什么？ | 叶子后的配置串，如 `main=50,pre=-1`，交给 Sparse。 |
| keyMap 和命令表区别？ | 命令表路由函数；keyMap 路由到 info 字段写回。 |
| 5993 是什么？ | 演示里的具体芯片 PortDrv/MACsec 实现名，可替换注册。 |
| 宏分发为什么包一层？ | 统一空指针检查与调用约定，业务不手写查表。 |
| 个人项目代码能否外传？ | 仓库是学习/演示向简化实现，面试讲清与真实产品差异即可。 |

---

## 10. 面试前自检清单

- [ ] 能默画：命令树 → TxParamSet → Get/Sparse/Set → Adapt → ops → 驱动  
- [ ] 能解释 Sparse 为什么必须先 Get  
- [ ] 能解释两次下发的**硬件动机**，不是「随便发两次」  
- [ ] 能区分 PortDrv 与 MACsec：域不同、挂钩子方式相同  
- [ ] 能举一个排障例子（配了不生效怎么查）  
- [ ] 简历表述与本文一致：串联与实现，不虚构无依据指标  

---

## 附：推荐口述顺序（被问「讲讲项目」时）

1. 背景：交换机端口诊断，文本命令要安全、可扩展地下到芯片。  
2. 方案：表驱动命令树 + 稀疏更新 + 函数指针分层。  
3. 细节：`0/96` 与 FunDes；Get→Sparse→Set；两次下发。  
4. 收获：解耦、表驱动扩展、对硬件副作用做软件容错。  
